
<div class="container-dresses">

  <h1>Les robes disponibles à <%= params[:city].upcase %></h1>

  <%= render "shared/filters_form" %>

  <% @dresses.each do |dress| %>
    <%= render partial: "shared/dresses_card", locals: {dress: dress} %>
  <% end %>

</div>
